<div class="max-w-xl mx-auto p-6 bg-white shadow-md rounded-lg mt-10 space-y-4">
    <h2 class="text-2xl font-bold text-center text-black">Tenant Dashboard</h2>

    @if ($isAuthenticated && $user)
        <div class="text-green-700 font-semibold">You are logged in as:</div>
        <ul class="text-black list-disc pl-5">
            <li><strong>Name:</strong> {{ $user['name'] }}</li>
            <li><strong>Email:</strong> {{ $user['email'] }}</li>
            <li><strong>User ID:</strong> {{ $user['id'] }}</li>
        </ul>
        <button wire:click="logout"
                class="mt-4 px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700">
            Logout
        </button>
    @else
        <p class="text-red-600">You are not authenticated.</p>
    @endif

    <hr>

    <div>
        <h3 class="font-semibold text-black">Session Details:</h3>
        <ul class="text-sm text-black break-words list-disc pl-5">
            <li><strong>Session ID:</strong> {{ $sessionId }}</li>
            <li><strong>CSRF Token:</strong> {{ $csrfToken }}</li>
            <li><strong>Cookie:</strong> {{ $cookieValue }}</li>
            <li><strong>Domain:</strong> {{ $domain }}</li>
        </ul>
    </div>
</div>
